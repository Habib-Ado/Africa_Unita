# ============================================
# Africa Unita - Variabili d'ambiente
# Copia questo file in .env e compila i valori
# NON committare mai .env (contiene dati sensibili)
# ============================================

# Database (per Railway usa DATABASE_URL)
DATABASE_URL=mysql://user:password@host:port/railway
# Oppure per locale:
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=railway
# DB_USER=root
# DB_PASSWORD=

# JWT (genera con: npm run db:generate-secrets)
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=7d

# Server
PORT=8080
NODE_ENV=development

# Upload (per Railway: monta un Volume e imposta es. UPLOAD_DIR=/data per persistere avatar)
# UPLOAD_DIR=/data

# Email (Nodemailer - Gmail)
EMAIL_USER=your-email@gmail.com
# Usa EMAIL_PASS o EMAIL_PASSWORD. Per Gmail: usa "App Password" da https://myaccount.google.com/apppasswords
EMAIL_PASS=
# EMAIL_PASSWORD=  # alternativo a EMAIL_PASS
